# GMOコイン API 使用方針

**作成日**: 2026-01-11
**対象フェーズ**: Phase 3（ルールエンジン）、Phase 4（自動売買）

---

## 概要

GMOコインのREST APIを使用して、テクニカル分析と自動売買を実装します。このドキュメントでは、各フェーズで使用するAPIとその用途を整理します。

---

## GMOコイン API 基本情報

### エンドポイント

- **公開API**: `https://forex-api.coin.z.com/public/v1`
- **プライベートAPI**: `https://forex-api.coin.z.com/private/v1`

### レート制限

- **GET リクエスト**: 6リクエスト/秒
- **POST リクエスト**: 1リクエスト/秒

### 認証

- **公開API**: 認証不要
- **プライベートAPI**: APIキー + シークレットキーによる署名認証

---

## Phase 3: ルールエンジン（テクニカル分析）

### 目的

収集したニュース分析結果と、テクニカル指標を組み合わせてトレードシグナルを生成する。

### 使用するAPI

#### 1. **ローソク足API（KLines）** - メイン

```
GET /public/v1/klines
```

**用途:**
- テクニカル指標の計算（MA, RSI, MACD等）
- トレンド判定
- 過去データの取得

**パラメータ:**
```python
{
    "symbol": "USD_JPY",        # 通貨ペア
    "priceType": "ASK",         # BID or ASK
    "interval": "1hour",        # 時間足
    "date": "20260111"          # 日付（YYYYMMDD）
}
```

**利用可能な時間足:**
- 短期: `1min`, `5min`, `10min`, `15min`, `30min`, `1hour`
- 中期: `4hour`, `8hour`, `12hour`
- 長期: `1day`, `1week`, `1month`

**レスポンス:**
```json
{
    "openTime": "1618588800000",  // Unix timestamp (ms)
    "open": "141.365",
    "high": "141.368",
    "low": "141.360",
    "close": "141.362"
}
```

**実装イメージ:**
```python
# 1時間足データを100本取得してRSI計算
klines = gmo_client.get_klines(
    symbol="USD_JPY",
    interval="1hour",
    limit=100
)

# ローソク足からRSI計算
closes = [float(k['close']) for k in klines]
rsi = calculate_rsi(closes, period=14)
```

---

#### 2. **Ticker API** - 補助的に使用（オプション）

```
GET /public/v1/ticker
```

**用途:**
- 最新の価格情報（現在値）を取得
- スプレッドの確認

**必要性の判断:**

**不要な理由:**
- ローソク足APIで最新のclose価格が取得できる
- テクニカル指標計算には過去データが必要（ローソク足で十分）

**使用する場合:**
- リアルタイムの価格をダッシュボードに表示したい場合
- 最新のスプレッドを確認したい場合

**結論: Phase 3では不要、Phase 5（ダッシュボード）で検討**

---

### Phase 3 の処理フロー

```
1. Firestoreから最新ニュース5件を取得
   ↓
2. ニュース分析結果を集計
   - センチメント平均: 1.8
   - 影響度: USD/JPY = 5, EUR/JPY = 3
   ↓
3. GMOコインからローソク足取得（1時間足 × 100本）
   ↓
4. テクニカル指標を計算
   - MA20, MA50（移動平均）
   - RSI（14期間）
   - MACD（12, 26, 9）
   ↓
5. 統合判定ロジック
   if センチメント > 1.5 AND 影響度 >= 4 AND MA20 > MA50 AND RSI < 70:
       signal = "BUY"
   ↓
6. トレードシグナルをFirestoreに保存
```

---

## Phase 4: 自動売買機能

### 目的

ルールエンジンが生成したトレードシグナルに基づいて、実際に売買を実行する。

### 使用するAPI

#### 1. **注文API（Order）**

```
POST /private/v1/order
```

**用途:**
- 新規注文の発注
- ストップロス設定

**パラメータ:**
```python
{
    "symbol": "USD_JPY",
    "side": "BUY",              # BUY or SELL
    "executionType": "MARKET",  # MARKET or LIMIT
    "size": "1000"              # 取引数量（通貨単位）
}
```

#### 2. **ポジション照会API（Positions）**

```
GET /private/v1/openPositions
```

**用途:**
- 現在保有中のポジション確認
- 含み損益の確認

#### 3. **注文キャンセルAPI（Cancel Order）**

```
POST /private/v1/cancelOrder
```

**用途:**
- 未約定の注文をキャンセル

#### 4. **決済API（Close Position）**

```
POST /private/v1/closeOrder
```

**用途:**
- ポジションの決済（利確/損切り）

---

### Phase 4 の処理フロー

```
1. ルールエンジンからシグナル取得
   signal = "BUY", confidence = 0.85
   ↓
2. リスク管理チェック
   - 口座残高確認
   - ポジションサイズ計算（残高の0.2%）
   - 最大損失チェック（1,000円/取引）
   ↓
3. GMOコインに注文発注
   POST /private/v1/order
   {
       "symbol": "USD_JPY",
       "side": "BUY",
       "executionType": "MARKET",
       "size": "1000"
   }
   ↓
4. ストップロス設定
   entry_price = 141.50
   stop_loss = entry_price - 0.20 (20pips)
   ↓
5. ポジション監視（定期実行）
   - 含み損益チェック
   - トレーリングストップ調整
   ↓
6. 決済条件に達したら決済
   POST /private/v1/closeOrder
```

---

## API使用方針まとめ

### Phase 3（ルールエンジン）で必要なAPI

| API | 用途 | 優先度 | 認証 |
|-----|------|--------|------|
| **KLines（ローソク足）** | テクニカル指標計算 | ✅ 必須 | 不要 |
| Ticker | リアルタイム価格 | ⭕ 不要 | 不要 |

**結論: ローソク足APIのみで実装可能**

### Phase 4（自動売買）で必要なAPI

| API | 用途 | 優先度 | 認証 |
|-----|------|--------|------|
| **Order（注文）** | 売買執行 | ✅ 必須 | 必要 |
| **Positions（ポジション照会）** | 保有確認 | ✅ 必須 | 必要 |
| **Close Order（決済）** | ポジション決済 | ✅ 必須 | 必要 |
| Cancel Order | 注文キャンセル | 🔶 推奨 | 必要 |

---

## 実装の優先順位

### Phase 3（現在）
1. ✅ **ローソク足API統合**
   - GMOコインクライアント作成
   - ローソク足取得機能
   - テクニカル指標計算（MA, RSI, MACD）

2. ✅ **ルールエンジン実装**
   - ニュース分析 + テクニカル指標の統合
   - シグナル生成ロジック
   - Firestoreへのシグナル保存

### Phase 4（将来）
1. 🔜 **注文API統合**
   - APIキー管理
   - 署名認証実装
   - 注文発注機能

2. 🔜 **リスク管理**
   - ポジションサイジング
   - ストップロス自動設定
   - 資金管理

3. 🔜 **ポジション監視**
   - 定期的なポジションチェック
   - トレーリングストップ
   - 自動決済

---

## テクニカル指標の計算ライブラリ

### 選択肢

1. **pandas-ta**（推奨）
   - Pandasと統合
   - 豊富な指標（130+）
   - シンプルなAPI

2. **TA-Lib**
   - 定番ライブラリ
   - 高速
   - インストールがやや複雑

3. **自前実装**
   - 軽量
   - カスタマイズ自由
   - メンテナンスコスト

**推奨: pandas-ta**（pandasとの相性が良く、実装が簡単）

---

## コスト・レート制限対策

### レート制限対策

**制限:**
- GET: 6リクエスト/秒
- POST: 1リクエスト/秒

**対策:**
1. **データキャッシュ**
   - 1時間足データは1時間キャッシュ
   - 無駄なリクエストを削減

2. **バッチ処理**
   - 複数通貨ペアを順次処理
   - リクエスト間に遅延を入れる

3. **エラーハンドリング**
   - レート制限エラー時は指数バックオフ
   - リトライロジック実装

### コスト

- **公開API**: 無料
- **プライベートAPI**: 無料（取引手数料は別途）

---

## 次のステップ

### Phase 3 実装タスク

1. GMOコインクライアント作成
2. ローソク足取得機能実装
3. テクニカル指標計算（pandas-ta使用）
4. ルールエンジン実装（ニュース + テクニカル統合）
5. シグナル生成・保存機能

**Ticker APIは不要、ローソク足APIのみで進める**

---

## 参考リンク

- [GMOコイン API ドキュメント](https://api.coin.z.com/fxdocs/)
- [pandas-ta ドキュメント](https://github.com/twopirllc/pandas-ta)

---

**作成者**: Claude Sonnet 4.5
**最終更新**: 2026-01-11
